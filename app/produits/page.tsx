import { BusinessApp } from "@/components/business-app";

export default function ProductsPage() {
  return (
    <BusinessApp
      page="placeholder"
      title="Produits"
      placeholder="Aucun calcul de coût produit n'est activé pour cette phase."
    />
  );
}
