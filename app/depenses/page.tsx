import { BusinessApp } from "@/components/business-app";

export default function ExpensesPage() {
  return (
    <BusinessApp
      page="expenses"
      title="Dépenses"
      placeholder="Lecture de l'onglet DEPENSES, sans modification de la structure."
    />
  );
}
