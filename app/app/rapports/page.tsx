import { BusinessApp } from "@/components/business-app";

export default function AppReportsPage() {
  return (
    <BusinessApp
      page="placeholder"
      title="Rapports"
      placeholder="Rapports détaillés — module en construction."
    />
  );
}
